<% if(alerts.error){%>
	<% alerts.error.forEach(function(item){%>
		<div class="alert alert-danger">
			<%= msg %>
		</div>
	<%});%>
<% } %>
<% if(alerts.success){%>
	<% alerts.success.forEach(function(item){%>
		<div class="alert alert-success">
			<%= msg %>
		</div>
	<%});%>
<% } %>